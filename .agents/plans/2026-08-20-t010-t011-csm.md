format: csm-plan/1
# T010 + T011 Remaining Work CSM Plan

## How To Execute
- Start work only through a separate, explicit csm-build invocation naming this plan; the planning session must not begin execution.
- Commit policy and live state are maintained in Control by csm-build.
- Risk summary: 16 tasks — 4 high (R1 csm-browse security/concurrency runtime, R7 bootstrap trust/signing, R8 check-suite gate surgery, J5 plan-signal lint), 1 standard per remaining, low where noted. High-risk tasks always require independent review. The check-suite single-writer chain J1 -> J4 -> J5 -> R8 serializes the only contended file; all other Wave-A tasks are disjoint per the per-file ownership table below.

## Per-File Ownership Table (Wave A)

| File | Owner(s) |
|---|---|
| csm-browse/scripts/ensure-browser.mjs | R1 (F-001, F-010, F-011, F-016-hardening, F-020, F-021-eb, F-067-8/10, F-065-d) |
| csm-browse/lib/docker.mjs | R1 (F-012, F-065-a, F-068-2 seam extraction) |
| csm-browse/scripts/session-daemon.mjs | R1 (F-019, F-065-c) |
| csm-browse/lib/daemon-core.mjs | R1 (F-008, F-014) |
| csm-browse/lib/verbs/record.mjs | R1 (F-006) |
| csm-browse/lib/verbs/log.mjs | R1 (F-004, F-005, F-067-7) |
| csm-browse/lib/verbs/close.mjs | R1 (F-013, F-067-13a close copy) |
| csm-browse/lib/recorder.mjs | R1 (F-007, F-067-6/11b) |
| csm-browse/lib/collectors.mjs | R1 (F-067-11a) |
| csm-browse/scripts/cdp-gate.mjs | R1 (F-017) |
| csm-browse/lib/cdp.mjs | R1 (F-067-3) |
| csm-browse/scripts/browse.mjs | R1 (F-067-2) |
| csm-browse/lib/constants.mjs | R1 (F-016 flags, F-067-5/9, F-059 IMAGE pin) |
| csm-browse/SKILL.md | R1 (container-hardening doc wording only) |
| csm-browse/lib/sweep.mjs | R2 (F-009, F-015, F-021-sweep, F-067-4/14) |
| csm-browse/lib/ports.mjs | R2 (F-018, F-067-15) |
| csm-browse/lib/cleanup.mjs | R2 (F-021-cleanup, F-067-13b cleanup copy) |
| csm-browse/package.json | R3 (F-058 jimp, F-060 csm-browse floor verify) |
| csm-browse/pnpm-lock.yaml | R3 (pnpm install regen) |
| csm-browse/scripts/check-skill.mjs | R3 (F-058 imported-deps resolve) |
| csm-browse/tests/e2e.mjs | R3 (F-041 REQUIRE, F-059 probe, F-068 bridge detection) |
| csm-browse/tests/serve.mjs | R3 (F-068 bridge detection) |
| csm-browse/tests/unit/recorder.test.mjs (new) | R3 (F-040) |
| csm-browse/tests/unit/cleanup.test.mjs (new) | R3 (F-040) |
| csm-browse/tests/unit/capture.test.mjs (new) | R3 (F-040) |
| csm-browse/tests/unit/fuzz.test.mjs (new) | R3 (F-042) |
| csm-browse/tests/unit/security-gate.test.mjs (new) | R1 (F-001, F-021-eb regression) |
| csm-browse/tests/unit/sweep-ports.test.mjs (new) | R2 (F-009/15/18/21) |
| csm-browse/tests/unit/auth.test.mjs | R9 (F-068 sleeps + seam caller re-point) |
| csm-browse/tests/unit/ports.test.mjs | R9 (F-068 seam caller re-point) |
| csm-browse/tests/unit/sweep.test.mjs | R9 (F-068 seam caller re-point) |
| csm-browse/tests/unit/security.test.mjs | R9 (F-068 sleeps) |
| csm-browse/tests/unit/security.test.mjs | R9 (F-068 sleeps) |
| csm-scan/lib/scan/deep/security.mjs | R4 (F-002, F-022, F-023, F-025) |
| csm-scan/lib/scan/providers/rules.mjs | R4 (F-002, F-031) |
| csm-scan/lib/scan/shared/manifest.mjs | R4 (F-027) |
| csm-scan/lib/scan/shared/detection.mjs | R4 (F-030) |
| csm-scan/lib/scan/shared/privacy.mjs | R4 (F-025) |
| csm-scan/lib/scan/report/reporter.mjs | R4 (F-025) |
| csm-scan/lib/scan/report/verbose-trace.mjs | R4 (F-051) |
| csm-scan/lib/scan/render/base.mjs | R4 (F-024) |
| csm-scan/lib/scan/render/practices.mjs | R4 (F-029) |
| csm-scan/lib/scan/write.mjs | R4 (F-024 fences, F-065-b NORMS tmp+rename) |
| csm-scan/lib/scan/pipeline/run.mjs | R4 (F-026, F-028) |
| csm-scan/lib/scan/deep/data/extractor.mjs | R4 (F-055) |
| csm-scan/lib/scan/deep/{config,conventions,testing,documentation,operations}.mjs | R4 (F-022, F-023, F-062) |
| csm-scan/scripts/scan.mjs | R4 (F-051) |
| csm-scan/SKILL.md | R4 (F-051 verbose wording, F-069-6 17-dimension intro) |
| .gitignore | R4 (F-051) |
| csm-scan/test/** (all edits) | R5 (F-032..F-039, F-056, F-068 scan components) |
| csm-upload/scripts/upload.mjs | R6 (F-048, F-064-1/2) |
| csm-upload/tests/upload.test.mjs | R6 (F-048 tests, F-068 upload happy-path) |
| bootstrap/fixtures/tls/key.pem | R6 (F-064-3 removal) |
| tests/package-audit.test.mjs | R6 (F-064-4 ok===false, F-068 integration-tier) |
| tests/protocol/engine.mjs | R7 (F-043/44/45/46/47/49) |
| tests/protocol/protocol.test.mjs | R7 (F-043/44/49) |
| tests/bootstrap-trust.test.mjs | R7 (F-044/45/46) |
| tests/offline/** | R7 (F-044 shared module, F-068 offline seam) |
| tests/integration/bootstrap-flow.test.mjs | R7 (acceptance-relevant; read-only unless shared-module import needs it) |
| bootstrap/package.json | R7 (files list: keyring + validator; F-060 bootstrap floor) |
| bootstrap/schema.json | R7 (F-046) |
| bootstrap/package/bin/csm-skills-bootstrap.js | R7 (F-043/45) |
| bootstrap/protocol.md | R7 (F-043/45 doc) |
| bootstrap/keyring.json | R7 (ship into package files) |
| scripts/pack-bootstrap.mjs | R9 (F-065-e) |
| scripts/check-suite.mjs | chain only: J1 -> J4 -> J5 -> R8 |
| scripts/lib/plan-validation.mjs | R8 (F-050 debt prune; F-054 canonical source) |
| scripts/sync-skill-boilerplate.mjs | R8 (F-054 migration, F-069-1 fail-loudly) |
| scripts/gen-readme-matrix.mjs | R8 (F-054 migration) |
| scripts/hooks/test/pre-commit.test.mjs | J2 (extend, Wave A) then R8 (un-stub, Wave D — serialized) |
| .lefthook.yml | J2 (baseline job) |
| .agents/docs/gate-baselines.json | J2 |
| .agents/docs/deferred.md | J4 |
| .agents/docs/journal-lessons.md | J7 |
| .agents/README.md | J4 (deferred line), J7 (journal-lessons + gate-baselines lines) |
| README.md | R8 (F-061 TOC, F-069-4 claim collapse) |
| bootstrap/runtime-commands.json | R8 (F-069-3) |
| scripts/with-node22.mjs | J3 |
| scripts/close-plan.mjs | J6 |
| scripts/check-plan-signals.mjs | J5 |
| tests/check-suite.test.mjs (new) | R8 (F-003 harness) |
| scripts/lib/contracts.mjs | J1/J5 conditional (constants only) |

## Control
- Plan ID: t010-t011-2026-08-20
- Status: ready
- Current CSM state: NOT_STARTED
- Cycle: 0
- Commits: allowed
- Last model/run: deepseek-v4-flash — planning session (rounds 1-2 critique remediated)
- Last checkpoint: 2026-08-20 — drafted from the consolidated plan's T010/T011 scope (`.agents/plans/2026-08-19-consolidated-remaining-work-csm.md`, reopened; T012 verified complete by sibling build) + `.agents/reviews/2026-08-19-skills-review.md` (68 upheld findings; scouts verified current state at HEAD 78a4e63/207bd13) + `.agents/plans/2026-08-20-embrace-journal-learnings-csm.md` (7 tasks ported as J1-J7); critique round 1 returned 26 findings, all remediated (ownership table, path fixes, signal fixes, coverage mapping)
- Next transition: On a future explicit csm-build invocation, NOT_STARTED -> RECOVER
- Active tasks: none
- Blockers: none
- Resume: re-read Last checkpoint, latest journal row, Recovery notes of all non-COMPLETE tasks, Discovered Requirements, the ownership table, and the working-tree diff

## Goal
Execute the two pending tasks of the reopened consolidated plan in one build: (T010) remediate the 68 upheld findings of `.agents/reviews/2026-08-19-skills-review.md` per their remediation sketches (~60 remain open at HEAD), and (T011) complete the seven journal-learnings tasks, ending with every gate green.

Deliverables:
1. csm-browse remediation (F-001, F-004..F-021, F-040, F-041-non-CI, F-042, F-057..F-060, F-065-a/c/d, F-067, F-068-browse) with focused tests.
2. csm-scan remediation (F-002, F-022..F-039, F-051, F-055, F-056, F-062, F-065-b, F-068-scan) with focused tests.
3. csm-upload remediation (F-048, F-064, F-068-upload).
4. Bootstrap/engine remediation (F-043..F-047, F-049, F-060-bootstrap, F-068-offline) with conformance tests.
5. Gates/tests remediation (F-003 harness, F-052, F-053-corpus, F-054, F-061, F-069-remaining, F-050-debt-prune, F-065-e).
6. Journal-learnings: `checkPayloadDrift` in check-suite, `scripts/record-gate-baseline.mjs` + `.agents/docs/gate-baselines.json`, `scripts/with-node22.mjs`, `.agents/docs/deferred.md` + DEFERRED-citation rule, `scripts/check-plan-signals.mjs`, `scripts/close-plan.mjs`, `.agents/docs/journal-lessons.md`.
7. `.agents/README.md` index lines for all new docs.

Constraints:
- Deferred records stay deferred: CI (F-041 CI job, F-053 CI component, F-032 CI note), Chromium sandbox redesign (F-016 redesign half — Docker's default seccomp profile satisfies the profile component; recorded), publication/key rotation (F-045/F-046 hard enforcement, F-060 engine-strict remainder), eval harness, README plan. Non-deferred halves ship as specified in each task.
- check-suite.mjs single-owner per wave: J1 -> J4 -> J5 -> R8 serial.
- `scripts/pack-bootstrap.mjs` never runs concurrently with another pack-invoking process; bootstrap suites run with `--test-concurrency=1`.
- No new npm dependencies without explicit approval (F-042 uses a dependency-free seeded fuzz harness).
- Do not edit COMPLETE plans; do not touch csm-deep-research skill content; no other skills' SKILL.md text except the cited F-016/F-051 wording edits.
- All acceptance-signal code blocks bash -n clean, no `<...>` placeholders, no `; test $? -eq` under set -e, no directory-form `node --test <dir>` (Node 22 quirk).
- `.git`-less corpora (planted-defect tests) must never be filtered by the F-053 untracked-ignore (no git -> no filtering) — the corpus checks must still see planted files.

Exclusions: T005-T009 deferred records; csm-deep-research-specific work (complete); token-efficiency flip follow-ups (complete); any finding not in the 68 upheld set; new features beyond sketches.

## Acceptance Criteria
1. Every one of the 68 upheld findings has a disposition: remediated with evidence, or explicitly carved out (deferred-overlap/policy) with recorded reasoning. Zero findings silently dropped (F-069 component 2 recorded as already-resolved via the lefthook migration).
2. All component suites green: csm-browse unit (glob form), csm-scan serial suite, csm-upload tests, five bootstrap suites (`--test-concurrency=1`), resume-semantics, check-suite harness suite, new focused tests per finding cluster.
3. `node scripts/check-suite.mjs` exits 0 with the count invariant recorded (585 baseline + explained deltas).
4. Journal-learnings ACs 1-7 hold (payload drift gated; baselines machine-recorded; with-node22 one-command; deferred ledger with DEFERRED citations; plan-signal lint green on the non-COMPLETE corpus; close-plan automation dry-run proven in a sandbox; journal-lessons doc indexed).
5. `.agents/README.md` indexes all new artifacts; no COMPLETE plan edited; final `git status --porcelain` = intended set only.
6. Review gate: independent REVIEW passes on the four high-risk tasks (R1, R7, R8, J5) with all findings remediated or recorded.

## Current-State Evidence
- HEAD 207bd13 (consolidated-plan hygiene commit; prior 78a4e63). Baseline: `node scripts/check-suite.mjs` exit 0 — "OK — 9 skills, 585 checks". Token-efficiency toggle `{"enabled": false}` (user directive; volatile/budget checks skipped). Tree clean. Host node v20.20.2 default; v22.23.2 at `$HOME/.nvm/versions/node/v22.23.2/bin` (verified by critic round 1). csm-browse engines already `>=22 <25` (F-060 csm-browse half is a verification + record, not a bump).
- Scout statuses at 78a4e63 (read-only, three parallel scouts): csm-browse 0/25 fixed; csm-scan 0/21 fixed; cross-component: F-050 FIXED, F-063 FIXED in substance, F-069 3/6 fixed (remaining 1/3/4; component 2 resolved via lefthook migration — no standalone sync job in .lefthook.yml), F-049 partial (forbiddenEnvelopeKeys landed), F-054 partial (check-suite canonicalized; sync-skill-boilerplate.mjs:14-40 divergent), F-003/F-042/F-043..F-048/F-052/F-053-corpus/F-061/F-064/F-065/F-068 open.
- Path corrections from critique: `csm-browse/lib/ensure-browser.mjs` does not exist — real path `csm-browse/scripts/ensure-browser.mjs`; `bootstrap/package/package.json` does not exist — real manifest `bootstrap/package.json` (no lockfile, zero deps); `csm-scan/lib/scan/render/write.mjs` does not exist — real `csm-scan/lib/scan/write.mjs`; F-055 target `deep/data/extractor.mjs` (the review's cited `shared/extractor.mjs` never existed).
- Same-defect double-assignments resolved: docker.mjs:165 spawn-'error' swallow is one defect shared by F-067-1 and F-065-a (owned by R1 once); session-daemon claimPidFile is shared by F-019 and F-065-c (R1); ensure-browser error-reporting shared by F-011 and F-065-d (R1).
- Journal-learnings plan (2026-08-20-embrace-journal-learnings-csm.md) is the verbatim spec source for J1-J7 (its T001-T007), including the T002 superseded-pointer note (lefthook job, not the generated shim) and T005's temp-dir-plant coordination note (harness lands later in R8).
- Node 22 quirk: directory-form `node --test tests/unit` fails on Node 22 ("Cannot find module") — glob form required (verified empirically by critic round 1). Review's F-054 canonical fenceMap lives in scripts/lib/plan-validation.mjs (imported by check-suite).

## Assumptions And Decisions
| ID | Statement | Type | Evidence or rationale | Status |
|---|---|---|---|---|
| D1 | T010 + T011 execute in ONE plan (user direction); journal-learnings plan is the J1-J7 spec source | user-dictated | User: "build a plan for 10 and 11 first. one plan." | agreed |
| D2 | Findings already remediated at HEAD are recorded, not re-implemented (F-050, F-063, F-069-2/5/6, F-049 partial, F-054 partial, F-056 partial) | decision | Scout + critique evidence | agreed |
| D3 | Deferred-overlap carve-outs ship only their non-deferred halves: F-041 REQUIRE flag (CI job deferred); F-053 corpus filter (CI deferred); F-016 cap-drop/no-new-privs/rootfs + Docker-default-seccomp note (sandbox redesign deferred); F-032 local gate hardening (CI note deferred) | decision | Deferred records T005/T006; scout deferral flags | agreed |
| D4 | F-045/F-046/F-049: validator + keyring shipped, envelope binds payload_index_sha256, TRUST verifies signature/keyring/expiry/steps-digest WHEN PRESENT; hard-requiring the signature stays publication-gated (T009) | decision | F-045/46/49 sketches; T009 deferral | agreed |
| D5 | F-042 property/fuzz tests are dependency-free (seeded deterministic generators) in a NEW fuzz.test.mjs | decision | Repo dep constraint; auth.test.mjs is R9-owned | agreed |
| D6 | F-026/F-034/F-035 implemented per sketch, tightly scoped (F-026 explicit legacy allowlists; F-034 behavioral constraints with small dated allowlists; F-035 golden set + mirror as cross-check) | decision | Sketches; challenger notes | agreed |
| D7 | F-055 targets `deep/data/extractor.mjs` | decision | Scout unknown-2; critique path check | agreed |
| D8 | F-021 split by file (R1: ensure-browser.mjs; R2: sweep.mjs + cleanup.mjs); F-067 split by component per the ownership table; F-065-a/c/d folded into R1 (same defects as F-067-1/F-019/F-011) | decision | Critique findings 1/12 | agreed |
| D9 | check-suite single-writer chain J1 -> J4 -> J5 -> R8; J5 uses temp-dir planted tests (harness lands in R8) | decision | Journal lesson D10; journal-learnings T005 note | agreed |
| D10 | F-050's inert PENDING_DEBT entries (plan-validation.mjs ~99-104, 5 entries) pruned in R8 — re-locate at execution | decision | Scout: inert, zero functional effect | agreed |
| D11 | F-069: component 2 recorded already-resolved (lefthook migration); component 5 already fixed (RECOVER ordinals); component 6 OPEN at HEAD -> R4 (csm-scan SKILL.md 17-dimension intro); components 1/3/4 land in R8 | decision | Scout partial + critique round-2 F4 | agreed |
| D12 | J2's baseline job targets `.lefthook.yml` (check-suite-preceding job), not the generated shim | decision | Journal-learnings T002 superseded-pointer | agreed |
| D13 | No edits to COMPLETE plans; `.agents/README.md` lines appended only | decision | Plan corpus rule | agreed |
| D14 | F-060 re-homed: csm-browse half = verify engines >=22 <25 + record (already satisfied; ceiling/engine-strict remainder carved out, publication-gated); bootstrap floor -> R7 | decision | Critique findings 3/14; real manifest evidence | agreed |
| D15 | R8's F-053 untracked-filter is gated on a `.git` at the corpus root (no git -> no filtering) so planted-defect corpora stay visible to the corpus checks | decision | Critique finding 20 | agreed |
| D16 | F-068 components partitioned: browse seams -> R9 (auth/security test edits); e2e/serve bridge detection -> R3; scan components -> R5; upload + package-audit -> R6; offline -> R7 | decision | Critique finding 5/7/8 | agreed |

## R&D Record
| ID | Question | Method/tool | Isolation and no-change evidence | Observation | Plan implication |
|---|---|---|---|---|---|
| RD-1 | Which findings are open at HEAD? | Three parallel read-only scouts diffing review SHA 61445e6..HEAD | Read-only git + source inspection | 0/25 browse, 0/21 scan open; cross: F-050/F-063 fixed, F-049/54/69 partial | D2 task scoping |
| RD-2 | Does directory-form `node --test <dir>` work on Node 22? | Critique round-1 empirical run | Read-only (temp invocation) | Fails: "Cannot find module '/.../tests/unit'" | Glob-form acceptance signals mandated |
| RD-3 | Real paths for cited files | grep the review citations vs repo tree | Read-only | ensure-browser.mjs is under scripts/; write.mjs under lib/scan/; bootstrap manifest is package.json | Ownership table path corrections |
| RD-4 | Baseline gate state | `node scripts/check-suite.mjs` | Read-only run | 585 checks exit 0 at HEAD | AC3 baseline |

## Discovered Requirements
- Any task editing `scripts/check-suite.mjs` serializes behind the previous owner (J1 -> J4 -> J5 -> R8).
- `scripts/pack-bootstrap.mjs` never runs concurrently with another pack-invoking process; bootstrap suites always `--test-concurrency=1`.
- New check-suite rules ship planted-defect negative tests (complete temp corpus + specific-message assertions, never exit-1 alone); F-053's untracked-filter must not apply to `.git`-less corpora (D15).
- The count invariant is a count, not a literal (585 baseline; deltas recorded).
- `.agents/docs/**` is unvalidated: new docs need only `.agents/README.md` index lines.
- Acceptance signals: bash -n clean; no `<...>` placeholders; no `; test $? -eq` under set -e; no `grep -q "$m"` over dash-leading tokens; no directory-form `node --test`; zsh-safe (pipefail or captured `$?`, never `PIPESTATUS`).
- Node >=22 for all gates (J3's with-node22 helper enforces; the F-003 harness in R8 runs under it).
- csm-browse e2e stays Docker-gated; the unit suite must pass without Docker.
- Every finding carries a disposition line in the build journal (AC1).

## Design
One plan, two halves, one serialization chain, one ownership table (above):
- **T011 half (J1-J7)** — ported verbatim from the journal-learnings plan (its spec is authoritative). Wave A parallel: J1, J2, J3, J6, J7. Wave B: J4 (after J1). Wave C: J5 (after J4). J3 drops all package.json/lockfile edits (F-060 re-homed, D14); J2 targets .lefthook.yml (D12).
- **T010 half (R1-R9)** — grouped by component; file ownership per the table (critique-remediated). R1 absorbs F-065-a/c/d (same defects as its F-067/F-019 items). R4 owns csm-scan runtime incl. write.mjs (F-024 + F-065-b). R5 owns ALL csm-scan test edits (incl. F-068 scan components). R6 owns upload + package-audit (F-048/64 + F-068 upload/integration-tier). R7 owns bootstrap/engine (F-043..F-047/49 + F-060-bootstrap + F-068-offline). R8 is the check-suite chain terminal (F-003 harness, F-052, F-053-corpus, F-054, F-061, F-069-1/3/4, F-050-debt-prune). R9 = F-065-e + F-068 browse seams (auth/security test edits only).
- **Wave A** (parallel, disjoint per the table): R1, R2, R3, R4, R5, R6, R7, R9, J1, J2, J3, J6, J7. **Wave B**: J4. **Wave C**: J5. **Wave D**: R8. Final gate after R8.

## Execution Graph
```text
[Wave A (parallel): R1 R2 R3 R4 R5 R6 R7 R9 J1 J2 J3 J6 J7]
[J1] --> [J4] --> [J5] --> [R8] --> final gate
```
- Wave A: 13 disjoint-ownership tasks per the per-file table (the only intra-finding splits are F-021/F-067/F-068 by file/component — D8/D16).
- Chain: J1 (check-suite) -> J4 -> J5 -> R8 (each owns check-suite.mjs alone in its wave).
- Critical path: J1 -> J4 -> J5 -> R8.

## Numbered Plan
1. [pending] csm-browse session/runtime hardening (F-001, F-004..F-008, F-010..F-014, F-016-hardening, F-017, F-019, F-020, F-021-eb, F-065-a/c/d, F-067 session parts)
   - Task ID: R1
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: high (F-001 unauthenticated CDP exposure; concurrency TOCTOUs; container config)
   - Owned scope: per the ownership table — `csm-browse/scripts/ensure-browser.mjs`, `csm-browse/lib/docker.mjs`, `csm-browse/scripts/session-daemon.mjs`, `csm-browse/lib/daemon-core.mjs`, `csm-browse/lib/verbs/record.mjs`, `csm-browse/lib/verbs/log.mjs`, `csm-browse/lib/verbs/close.mjs`, `csm-browse/lib/recorder.mjs`, `csm-browse/lib/collectors.mjs`, `csm-browse/scripts/cdp-gate.mjs`, `csm-browse/lib/cdp.mjs`, `csm-browse/scripts/browse.mjs`, `csm-browse/lib/constants.mjs`, `csm-browse/SKILL.md` (container-hardening wording only), new test file `csm-browse/tests/unit/security-gate.test.mjs`
   - Not in scope: sweep.mjs/ports.mjs/cleanup.mjs (R2); auth.test.mjs/security.test.mjs (R9); recorder/capture/cleanup unit breadth (F-040, R3); the sandbox redesign itself (deferred T006); CI
   - Spike candidate: F-001 — spike in a temp dir with the container running: verify the token-gated funnel rejects a bare `localhost:9222/json/version` probe (expect 401/close); record before committing the implementation; Docker-down => record environment-blocked and rely on the unit-level gate test
   - Actions: implement per the review sketches (quote each from `.agents/reviews/2026-08-19-skills-review.md` before editing):
     - F-001: stop publishing 9222 on the default bridge or run the token-gated funnel; bind the debug port to 127.0.0.1 inside the container; update the SKILL.md probe instructions to the gated form; unit regression in security-gate.test.mjs; spike evidence first.
     - F-004: push rotated files before mainPath or sort parsed events by timestamp; synthetic rotated+main unit test.
     - F-005: `await` hasAnyEventsFile (or make it sync); exit-2 regression test.
     - F-006: on command timeout, unlink the cmd file when unclaimed or write a tombstone; drop stale commands.
     - F-007: after SIGKILL, await 'exit' with a short hard bound before validating; probe container integrity.
     - F-008: wrap the result write; on failure move back to cmd/ or write an error result; blanket catch for readdir only.
     - F-010: EEXIST re-read — empty content: O_TRUNC no-follow overwrite or delete+retry O_EXCL once.
     - F-011: launchDaemon null -> non-zero exit or typed error at every call site (ensure-browser.mjs:549-635 region).
     - F-012: centralize the docker CLI timeout (15-60s default); apply to every docker subprocess.
     - F-013: close verb tracks failures distinctly from warnings; exit non-zero or surface the failed list.
     - F-014: wrap each queue command execution in withTimeout; error result on timeout.
     - F-059 (constants.mjs IMAGE pin): add a digest-age staleness check for the pinned image constant (the e2e probe change is R3's).
     - F-016 (non-redesign): add `--cap-drop=ALL`, `--security-opt no-new-privileges`, read-only rootfs; keep --no-sandbox; record that Docker's default seccomp profile covers the profile component; sandbox redesign deferred T006.
     - F-017: deny() destroys the socket after a grace timer (mirror serveStaticProtocol); clear the timer on close; remove from the tracked set.
     - F-019: claimPidFile — after rm, re-attempt O_EXCL with a deadline; daemon verifies inode ownership (same defect as F-065-c).
     - F-020: adoptSession writes creating.marker before killGate/spawnGate, removes it after saveState.
     - F-021 (ensure-browser.mjs part, :371-383): verify process identity (starttime or pgrep argv match) before signaling.
     - F-065-a: docker.mjs:165 — await spawn 'error' before returning pid (same defect as F-067-1).
     - F-065-d: ensure-browser.mjs:641 — redacted err.stack with cause.
     - F-067 (session parts): (2) browse.mjs:68-78 unknown-verb ERR_MODULE_NOT_FOUND masking fixed; (3) cdp.mjs:8-15 getSession process.exit(2) -> typed error; (5) single VNC-pass path source (constants.mjs:22 env-file); (6) activeRecording before first await; (7) log.mjs --values footgun; (8) port-lock release only after bind confirm; (9) VNC password via env-file not argv; (10) cgroup limits with the F-016 hardening; (11) bounded write queues (collectors.mjs:31, recorder.mjs:105-108); (12) rotation persist-before-kill ordering; (1) remove the dead on-disk 'already recording' guard (recorder.mjs:61-72 — F-067-1 is the dead guard, distinct from F-065-a's docker.mjs spawn-error swallow; do not conflate).
   - Acceptance signal: from csm-browse/ under node >=22: `node --test --test-concurrency=1 "tests/unit/*.test.mjs"` exits 0 including security-gate.test.mjs; the F-001 spike transcript recorded (bare probe rejected); `node scripts/check-suite.mjs` from repo root exits 0.
   - Validation: cheapest first — `node --check` each touched file; the F-001 spike (Docker); focused tests; full unit suite (glob form); check-suite.
   - Acceptance evidence: spike transcript, test outputs, per-finding implementation notes.
   - Repair attempts: 0
   - Recovery note: partial = some findings unimplemented; each finding is an independently revertible edit; the acceptance signal requires ALL owned findings' tests to pass — no partial completion.
   - (D3/D8) F-016 ships only the hardening half; F-021 covers only ensure-browser.mjs; F-065-a/c/d live here (same defects as F-067-1/F-019/F-011).

2. [pending] csm-browse sweep/ports/cleanup remediation (F-009, F-015, F-018, F-021-sweep/cleanup, F-067-cleanup/ports)
   - Task ID: R2
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: standard (process-kill sweep code; concurrency)
   - Owned scope: per the table — `csm-browse/lib/sweep.mjs`, `csm-browse/lib/ports.mjs`, `csm-browse/lib/cleanup.mjs`, new test file `csm-browse/tests/unit/sweep-ports.test.mjs`
   - Not in scope: ensure-browser.mjs (R1); close.mjs (R1 — its releasePorts copy is F-067-13a)
   - Spike candidate: none — sweep kill shapes were live-captured in the consolidated-plan cycle-1 spike; reuse its argv-shape evidence
   - Actions: per sketch —
     - F-009: distinguish read-missing from parse/validate failure; reap marker-only dirs older than CREATING_MARKER_MAX_MS; remove stale markers on claim.
     - F-015: reap marker-only dirs older than the marker TTL, removing the marker; aged-marker+missing-state.json sweepable.
     - F-018: break stale locks via atomic rename-and-inspect or inode compare; never unlink a lock you did not create (releasePortLock included — F-067-15).
     - F-021 (sweep.mjs:64-75, cleanup.mjs:29-43): record/verify process identity (starttime or pgrep argv match) before signaling.
     - F-067-4: sid revalidation per line inside the per-line try (sweep.mjs:147-172).
     - F-067-13b: cleanup.mjs's copy of the redundant releasePorts double killGate removed (close.mjs's copy is R1's).
     - F-067-14: recorder.json read-modify-write via temp+rename (sweep.mjs:225-248).
   - Acceptance signal: `node --test --test-concurrency=1 "tests/unit/*.test.mjs"` in csm-browse exits 0 with the new sweep/ports/cleanup tests; `node scripts/check-suite.mjs` from repo root exits 0.
   - Validation: cheapest first — node --check; focused tests; full unit suite (glob form); check-suite.
   - Acceptance evidence: test outputs; per-finding notes.
   - Repair attempts: 0
   - Recovery note: partial = some findings unimplemented; each revertible; no partial completion.
   - (D8) F-067-13a (close.mjs) belongs to R1 — do not touch close.mjs here.

3. [pending] csm-browse tests/supply remediation (F-040, F-041-non-CI, F-042, F-057, F-058, F-059, F-060-csm-browse-verify, F-068-e2e/serve)
   - Task ID: R3
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: low
   - Owned scope: per the table — `csm-browse/package.json`, `csm-browse/pnpm-lock.yaml` (pnpm install regen), `csm-browse/scripts/check-skill.mjs`, `csm-browse/tests/e2e.mjs`, `csm-browse/tests/serve.mjs`, new test files `csm-browse/tests/unit/{recorder,cleanup,capture,fuzz}.test.mjs`
   - Not in scope: auth.test.mjs/security.test.mjs (R9); constants.mjs (R1 owns F-059's IMAGE pin there); CI job (deferred); e2e Docker provisioning (deferred)
   - Spike candidate: none
   - Actions:
     - F-040: unit-test recorder start/stop with a stubbed ffmpeg (recorder.test.mjs); cleanup with exec stubs (cleanup.test.mjs); the capture verb with a fake CDP client (capture.test.mjs).
     - F-041 (non-CI half): e2e skips become non-zero when `CSM_BROWSE_E2E_REQUIRE=1`; default skip behavior unchanged; CI job deferred (record).
     - F-042: dependency-free seeded fuzz harness in fuzz.test.mjs (deterministic request-line generator; property: parsable or cleanly rejected, no throw/crash) + bounded mutation pass over record inputs; fast, no Docker.
     - F-057: record a CRI-release tracking note (upgrade only when CRI drops ws ^7.2.0); no blind upgrade.
     - F-058: remove the jimp dependency; check-skill resolves only imported deps; `pnpm install` lockfile regen.
     - F-059: constants.mjs IMAGE pin staleness check lives in R1's constants edits; here: change the e2e probe to the pinned IMAGE constant.
     - F-060 (csm-browse half): verify engines `>=22 <25` already satisfied; record the verification; the ceiling/engine-strict remainder is publication-gated (recorded carve-out).
     - F-068 (e2e/serve half): runtime bridge-gateway detection replacing 172.17.0.1 literals (e2e.mjs:31, serve.mjs:22).
   - Acceptance signal: csm-browse `node --test --test-concurrency=1 "tests/unit/*.test.mjs"` green with the new suites; `CSM_BROWSE_E2E_REQUIRE=1 node --test tests/e2e.mjs` exits non-zero on Docker-down (recorded) while the default (REQUIRE unset) run skips and exits 0; `node scripts/check-skill.mjs` passes; `rg '"jimp"' csm-browse` returns nothing; check-suite green.
   - Validation: cheapest first — pnpm install (csm-browse only), node --check, unit suite, e2e REQUIRE probe, check-skill, check-suite.
   - Acceptance evidence: test outputs, REQUIRE probe, lockfile diff, check-skill output.
   - Repair attempts: 0
   - Recovery note: partial = supply changes landed but tests missing; pnpm-lock.yaml regen must be re-run if package.json changes again; never hand-edit the lockfile.
   - (D5/D14/D16) F-042 in fuzz.test.mjs (new file); F-060 verified, not bumped; e2e/serve owned here.

4. [pending] csm-scan runtime remediation (F-002, F-022..F-031, F-051, F-055, F-062, F-065-b)
   - Task ID: R4
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: standard (F-002 is a ReDoS on attacker-controlled regex; parser changes)
   - Owned scope: per the table — all `csm-scan/lib/scan/**` runtime modules cited by the findings (deep/security.mjs, providers/rules.mjs, shared/manifest.mjs, shared/detection.mjs, shared/privacy.mjs, report/reporter.mjs, report/verbose-trace.mjs, render/base.mjs, render/practices.mjs, write.mjs, pipeline/run.mjs, deep/data/extractor.mjs, deep/{config,conventions,testing,documentation,operations}.mjs), `csm-scan/scripts/scan.mjs`, `csm-scan/SKILL.md` (F-051 verbose wording + F-069-6: rewrite the intro to name all 17 evidence dimensions — line 8 currently says 7+7=14), `.gitignore`, plus new focused test files under `csm-scan/test/` (NEW files only — existing test files are R5's)
   - Not in scope: existing csm-scan test-file edits (R5); CI; the F-026 gate extension is HERE (run.mjs)
   - Spike candidate: F-002 — spike in a temp corpus: feed a crafted `.gitleaks.toml` allowlist pattern to the compiled matcher; verify bounded evaluation time (or rejection) before/after
   - Actions: per sketch —
     - F-002: route `.gitleaks.toml` allowlist patterns through `validatePluginRegexSource` (or a shared regex-complexity validator) with length caps; match allowlist entries as literal globs; scan-level watchdog; spike evidence first.
     - F-022: statSync before read; skip files above the bound; bounded reader.
     - F-023: lstat/realpath containment before reading well-known files (mirror the plugin loader).
     - F-024: neutralize newlines + markdown-significant tokens in repo-controlled fields; delimited fences for description blocks (base.mjs + write.mjs:64/75/94).
     - F-025: unify report redactor with the deep scanner's token families; redact by span.
     - F-026: extend the structural privacy gate to legacy dimension models with explicit allowlists.
     - F-027: merge only workspace=true pool entries, or mark declared-pool provenance distinctly.
     - F-028: return the final enriched (or both, explicitly named).
     - F-029: escape kind tokens via escapeField({inTable:true}); drop the unused parameter.
     - F-030: restrict framework entries to specific auth/validation subsystems or a capability category.
     - F-031: per-rule cap breaks only the inner loop; total maxMatches stays the sole outer-break condition.
     - F-051: `.csm-scan-debug.log` -> .gitignore (or OS temp); per-run-unique trace name.
     - F-055: track source offsets through the split (or search full source); multi-statement SQL fixture. Target deep/data/extractor.mjs (D7).
     - F-062: shared bounded-read helper replacing per-scanner readFileSync sites.
     - F-065-b: writeNORMS via tmp+rename (write.mjs:135).
   - Acceptance signal: `cd csm-scan && node --test --test-concurrency=1` exits 0 including the new focused tests; the F-002 spike transcript recorded (crafted pattern bounded/rejected); `node scripts/check-suite.mjs` from repo root exits 0.
   - Validation: cheapest first — node --check; F-002 spike; focused tests; csm-scan serial suite (authoritative); check-suite.
   - Acceptance evidence: spike transcript, test outputs, per-finding notes.
   - Repair attempts: 0
   - Recovery note: partial = some findings unimplemented; each revertible; the F-002 fix is the gate — do not complete this task without it.
   - (D7) F-055 path is deep/data/extractor.mjs; (D8) F-065-b moved here (write.mjs single owner).

5. [pending] csm-scan test-quality remediation (F-032..F-039, F-056, F-068-scan)
   - Task ID: R5
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: standard (test-suite surgery; must keep the serial suite green)
   - Owned scope: ALL existing `csm-scan/test/**` file edits (the cited files + F-068 scan components: determinism suite sort-key import + self-digest removal)
   - Not in scope: new test files R4 creates (its owned scope); runtime code except exports needed by F-036 (add exports only, coordinate with R4 via a journal note)
   - Spike candidate: none
   - Actions: per sketch —
     - F-032: per-module coverage manifest; direct unit tests for render/git.mjs and verbose-trace.mjs; gate branch/function floors. (CI note deferred — local gate hardens.)
     - F-033: delete source-text assertions; enforce the dispatch rule structurally or behaviorally.
     - F-034: replace byte-hash self-locks with behavioral constraints where a constraint exists; small dated allowlists for the rest.
     - F-035: pin one golden set from a frozen revision; assert production against it; keep the mirror as cross-check only.
     - F-036: derive registry/case counts from production constants; drop self-referential case-count assertions.
     - F-037: extract BANNED_VOICE/stripNonProse into a shared helper imported by ALL suites (incl. the third copy at expansion-render-registration.test.mjs); pin the vocabulary in one equality test.
     - F-038: remove source-text/self-count assertions; collect registered test names via the runner.
     - F-039: extract canonicalize/fixedInput/semanticProjection into one shared helper; assert the normalization list against fixture-behavior.json.
     - F-056: drop the co-drift equality; keep/extend hand-computed per-dimension oracles.
     - F-068-scan: import the production sort key instead of re-implementing it; drop the DETERMINISM_EVIDENCE self-digest assertion.
   - Acceptance signal: `cd csm-scan && node --test --test-concurrency=1` exits 0 (full serial suite, count recorded); `node scripts/check-suite.mjs` from repo root exits 0.
   - Validation: cheapest first — focused test files; full serial suite (authoritative); check-suite.
   - Acceptance evidence: suite output with pass count; per-finding notes.
   - Repair attempts: 0
   - Recovery note: partial = some findings unimplemented; each test file revertible; never weaken a gate to appease a test.
   - (D6/D16) Tight-scoped F-034/35; F-068 scan components owned here.

6. [pending] csm-upload remediation (F-048, F-064, F-068-upload/package-audit)
   - Task ID: R6
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: standard (F-064 includes a committed TLS key — history rewrite requires approval)
   - Owned scope: per the table — `csm-upload/scripts/upload.mjs`, `csm-upload/tests/upload.test.mjs`, `bootstrap/fixtures/tls/key.pem` (removal), `tests/package-audit.test.mjs`
   - Not in scope: real remote pushes; GitHub Pages config; bootstrap/package.json (R7)
   - Spike candidate: none
   - Actions:
     - F-048: validate `github` `/^[A-Za-z0-9-]{1,39}$/` and `pagesRepo` `/^[A-Za-z0-9._-]+$/`; URL-class construction with hostname === 'github.com' assertion.
     - F-064: (1) mkdir 0700 + writeFile 0600 for the config; (2) drop 'svg' from allowed publish types; (3) TLS key: remove the committed fixture, generate ephemeral keys in test setup; purge the key from git history ONLY with explicit user approval at RECOVER (otherwise record the residual — the key is a TEST fixture, not a live secret); (4) tamper test asserts `verification.ok === false` (package-audit.test.mjs:116).
     - F-068-upload: real-remote upload happy-path via a local stub remote (no network); F-068-integration-tier: package-audit labeled with the pinned toolchain.
   - Acceptance signal: `node --test csm-upload/tests/upload.test.mjs` green with the new validation + happy-path tests; `node --test tests/package-audit.test.mjs` green with the ok===false assertion; `rg 'svg' csm-upload` no publish-type hits; check-suite green.
   - Validation: cheapest first — node --check; upload tests; package-audit; check-suite.
   - Acceptance evidence: test outputs; the key-handling decision recorded (approved purge vs residual note).
   - Repair attempts: 0
   - Recovery note: partial = validation landed, key handling pending a user decision at RECOVER — if the purge is not granted, complete the rest and record the residual (do not block the task).
   - (D16) F-068 upload + integration-tier components owned here.

7. [pending] bootstrap/engine remediation (F-043, F-044, F-045, F-046, F-047, F-049, F-060-bootstrap, F-068-offline)
   - Task ID: R7
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: high (trust/signing semantics; the shipped bootstrap artifact)
   - Owned scope: per the table — `tests/protocol/engine.mjs`, `tests/protocol/protocol.test.mjs`, `bootstrap/package/bin/csm-skills-bootstrap.js`, `bootstrap/schema.json`, `bootstrap/keyring.json` (ship into package files), `tests/bootstrap-trust.test.mjs`, `tests/offline/**`, `tests/integration/bootstrap-flow.test.mjs`, `bootstrap/package.json` (files list + F-060 floor), `bootstrap/protocol.md`
   - Not in scope: hard-requiring the Ed25519 signature (publication-gated, T009 deferred — D4); CI
   - Spike candidate: none — the trust tests already exercise the validator in test land
   - Actions:
     - F-043: conformance test deriving the protocol state table from protocol.md.
     - F-044: extract the shell denylist + fixed-package policy into one shared module imported by engine, trust test, and offline grammar.
     - F-045 (D4): ship the envelope validator + keyring into bootstrap/package (package.json files list); engine/validator verifies the Ed25519 signature over payload-index.json WHEN PRESENT (fail-closed on malformed); the no-signature local flow keeps working; hard-requirement recorded as publication-gated.
     - F-046: add required `payload_index_sha256` to the envelope schema; include it in policyToSign.
     - F-047: re-lstat every component immediately before each write; O_NOFOLLOW targets; tmp+rename inside a re-validated dir.
     - F-049: factor trust validation into the shared module; engine TRUST calls it (signature when present, keyring, expiry, steps digest) before MATERIALIZE.
     - F-060 (bootstrap half): engines floor -> >=22 in bootstrap/package.json (no lockfile exists there — no install step).
     - F-068-offline: offline tests use the pinned toolchain + the shared module import.
   - Acceptance signal: five bootstrap suites green from repo root with the exact command: `node --test --test-concurrency=1 tests/bootstrap-trust.test.mjs tests/package-audit.test.mjs tests/protocol/*.test.mjs tests/offline/*.test.mjs tests/integration/*.test.mjs` (glob form; the Makefile's canonical form plus --test-concurrency=1); the new conformance + trust tests pass; `node scripts/check-suite.mjs` exits 0; `node scripts/pack-bootstrap.mjs` re-run (payload refreshed) then the MAPPING-AWARE comparison (pack-bootstrap copies a subset, not whole dirs): `diff -q bootstrap/package/payload/skills/csm-browse/SKILL.md csm-browse/SKILL.md`; `diff -q bootstrap/package/payload/skills/csm-grill/SKILL.md csm-grill/SKILL.md`; same for csm-bdd-tdd, csm-build, csm-deep-research, csm-plan, csm-review, csm-upload/SKILL.md, csm-scan/SKILL.md; `diff -rq bootstrap/package/payload/skills/csm-scan/lib csm-scan/lib` + `diff -q bootstrap/package/payload/skills/csm-scan/scripts/scan.mjs csm-scan/scripts/scan.mjs`; `diff -q bootstrap/package/payload/skills/csm-upload/scripts/upload.mjs csm-upload/scripts/upload.mjs` — all must be empty. (J1's checkPayloadDrift is the same mapping inside check-suite; full re-verification at the final gate.)
   - Validation: cheapest first — node --check; focused protocol/trust tests; full five-suite battery (serial); pack re-run + diff loop; check-suite.
   - Acceptance evidence: test outputs, pack digest, diff output.
   - Repair attempts: 0
   - Recovery note: partial = some findings unimplemented; the pack re-run is mandatory after package.json files-list changes; never hand-edit payload-index.json.
   - (D4/D14/D16) Conditional-when-present signature; bootstrap floor here; offline seam here.

8. [pending] Gate/test-harness remediation (F-003, F-052, F-053-corpus, F-054, F-061, F-069-1/3/4, F-050-debt-prune)
   - Task ID: R8
   - Depends on: J5 (check-suite single-writer chain terminal; note: also co-edits scripts/hooks/test/pre-commit.test.mjs after J2's Wave-A extension — serialized by wave; re-locate line targets at execution)
   - Parallel group: G2 (Wave D)
   - Risk: high (gate surgery; must keep 585+ checks green and add a test harness)
   - Owned scope: per the table — `scripts/check-suite.mjs`, `scripts/lib/plan-validation.mjs` (PENDING_DEBT prune), `scripts/sync-skill-boilerplate.mjs`, `scripts/gen-readme-matrix.mjs`, `scripts/hooks/test/pre-commit.test.mjs` (un-stub), `tests/check-suite.test.mjs` (new harness), `README.md` (TOC + claim collapse), `bootstrap/runtime-commands.json` (F-069-3)
   - Not in scope: CI (deferred); the lefthook flow itself
   - Spike candidate: none — the temp-corpus planted-defect pattern is proven (J1/J4/J5)
   - Actions:
     - F-003: new `tests/check-suite.test.mjs` — node:test suite building complete minimal temp corpora (repo copy excluding .git/node_modules) with one-violation-per-rule fixtures, asserting exit codes + specific messages for the major rule families (frontmatter, machine, corpus, boilerplate, README, contracts, payload-drift, DEFERRED-citation, plan-signal); un-stub pre-commit.test.mjs:71 to invoke the real check-suite in the temp-repo harness (re-locate the line after J2's edits).
     - F-052: route CONTRACTS/UPLOAD_SCRIPT_REF/README scans through the canonical fenceMap; fix the README path-class boundary.
     - F-053 (corpus half, D15): corpus loops ignore untracked files ONLY when a `.git` exists at the corpus root (no git -> no filtering — planted corpora stay visible); CI component deferred (record).
     - F-054: migrate sync-skill-boilerplate.mjs and gen-readme-matrix.mjs to the canonical splitLines/fenceMap from scripts/lib/plan-validation.mjs; shared fence edge-case unit tests.
     - F-061: a check-suite rule verifies every H2 below the README TOC has a TOC entry.
     - F-069-1: sync --write fails loudly when a section is missing (instead of "rewrote 0"); F-069-3: replace the bare `--no` npx flag with `--no-` in bootstrap/runtime-commands.json; F-069-4: collapse the duplicated README release-status claim.
     - F-050 debt: prune the inert PENDING_DEBT entries (plan-validation.mjs:99-102); verify the template-format-marker checks still hard-enforce.
   - Acceptance signal: `node --test --test-concurrency=1 tests/check-suite.test.mjs tests/resume-semantics.test.mjs` green; `node scripts/check-suite.mjs` exits 0 with the count recorded (585 + deltas); the planted-violation corpus cases all exit 1 with specific messages (recorded); `node scripts/sync-skill-boilerplate.mjs --check` clean; `node scripts/gen-readme-matrix.mjs --check` clean.
   - Validation: cheapest first — node --check; new harness; resume-semantics; sync/matrix --check; check-suite full.
   - Acceptance evidence: harness output, per-rule violation transcripts, count delta record.
   - Repair attempts: 0
   - Recovery note: partial = harness landed but routing not; each sub-fix revertible; never weaken a rule to make the harness pass (the harness asserts the rule, not the reverse).
   - (D9/D10/D11/D15) Chain terminal; PENDING_DEBT prune; F-069 components 1/3/4; git-gated untracked filter.

9. [pending] Cross-component lows (F-065-e, F-068-browse-seams)
   - Task ID: R9
   - Depends on: none
   - Parallel group: G1 (Wave A)
   - Risk: low
   - Owned scope: per the table — `scripts/pack-bootstrap.mjs` (F-065-e), `csm-browse/tests/unit/auth.test.mjs` (F-068 sleeps + seam caller re-point), `csm-browse/tests/unit/security.test.mjs` (F-068 sleeps), `csm-browse/tests/unit/ports.test.mjs` + `csm-browse/tests/unit/sweep.test.mjs` (seam caller re-pointing only)
   - Not in scope: docker.mjs/session-daemon.mjs/ensure-browser.mjs (R1); csm-scan test edits (R5); e2e.mjs/serve.mjs (R3); upload/package-audit/offline tests (R6/R7)
   - Spike candidate: none
   - Actions:
     - F-065-e: pack-bootstrap.mjs — clean the staging dir on FAILURE (preserve the success-path return of the staging dir). Verification runs in a SANDBOXED COPY (copy the script to a temp dir, stub the failure, assert the staging dir is removed) — never a live pack run in parallel with R7 (pack-serial rule).
     - F-068 (browse seams): event-driven waits replacing fixed sleeps (auth.test.mjs:272, security.test.mjs:221); test-only exec seam (setExecLayerForTests) replacing module-global exec-layer mutation.
   - Acceptance signal: csm-browse unit suite green (`node --test --test-concurrency=1 "tests/unit/*.test.mjs"`); the sandboxed simulated pack-failure run leaves no staging dir behind (recorded); `node scripts/check-suite.mjs` exits 0.
   - Validation: cheapest first — node --check; focused tests; unit suite; pack-failure simulation; check-suite.
   - Acceptance evidence: test outputs, pack-failure transcript.
   - Repair attempts: 0
   - Recovery note: partial = some components unimplemented; each revertible; the pack fix must preserve the documented success-path return.
   - (D16) Only the browse-seam components of F-068 remain here; all other F-068 components are owned by R3/R5/R6/R7.

10. [pending] Payload-drift gate in check-suite.mjs
    - Task ID: J1
    - Depends on: none
    - Parallel group: G1 (Wave A)
    - Risk: standard
    - Owned scope: `scripts/check-suite.mjs` (checkPayloadDrift + main-gate call), `scripts/lib/contracts.mjs` (only if constants needed)
    - Not in scope: bootstrap/** content, pack-bootstrap.mjs, payload-index.json, other check-suite rules
    - Spike candidate: none — the consolidated T002 pinned comparison command is the proven spec; dynamic count
    - Actions: port the journal-learnings T001 verbatim: (1) `checkPayloadDrift(root)` mirroring `checkDrift`: walk `bootstrap/package/payload/skills/**` files only, map each to its repo-root source, sha256 both, collect DIFF/MISSING-SOURCE, print `{compared:N, issues:[]}` dynamically; (2) call from the main gate alongside checkDrift; (3) planted-defect test: complete minimal temp corpus, mutate one byte of one payload file, assert exit 1 AND the specific `DIFF <rel>` line; (4) real-repo run passes with `issues:[]`.
    - Acceptance signal: `node scripts/check-suite.mjs` exits 0 with `issues:[]` on the live payload AND the planted temp-root run exits 1 with the specific DIFF line (both recorded). NOTE: Wave-A siblings (R1/R4 SKILL.md edits) can stale the payload mirror until R7's pack — the live-payload leg is re-verified at INTEGRATE after the wave settles (F19).
    - Validation: cheapest first — node --check; planted run; real-repo run; dynamic-count proof (no literal count in the new code).
    - (F19) INTEGRATE re-runs this acceptance after Wave-A siblings settle.
    - Acceptance evidence: gate output (real + planted), temp-root transcript.
    - Repair attempts: 0
    - Recovery note: additive rule; if it false-positives on the live payload, fix the mapping, never the payload.
    - (Spec source: 2026-08-20-embrace-journal-learnings-csm.md T001 — authoritative.)

11. [pending] Gate-baseline recorder
    - Task ID: J2
    - Depends on: none
    - Parallel group: G1 (Wave A)
    - Risk: standard
    - Owned scope: `scripts/record-gate-baseline.mjs` (new), `.lefthook.yml` (one check-suite-preceding job — D12), `.agents/docs/gate-baselines.json` (first record), `scripts/hooks/test/pre-commit.test.mjs` (extend — R8 re-locates its un-stub target later)
    - Not in scope: check-suite.mjs; baseline hard-fail policy (warn, not fail)
    - Spike candidate: none
    - Actions: port the journal-learnings T002 verbatim incl. its superseded-pointer note (lefthook job, not the generated shim): `--record` appends {gate, passCount, wallMs, nodeVersion, ts}; `--check` compares against the latest record, non-zero on deviation beyond tolerance, warns on >30-day age; the lefthook job runs `node scripts/record-gate-baseline.mjs --check` guarded by existence of the script AND baseline file; the script runs check-suite itself and parses the "N checks" count; record the first baseline (585); extend pre-commit.test.mjs with a stubbed case. NOTE: J1's rule may shift the count — re-record the baseline at INTEGRATE (F15).
    - Acceptance signal: `node scripts/record-gate-baseline.mjs --record check-suite 585 1234` then `node scripts/record-gate-baseline.mjs --check check-suite 585 1200` exits 0; `node scripts/record-gate-baseline.mjs --check check-suite 700 1200` exits non-zero with a deviation message; standalone `node scripts/record-gate-baseline.mjs --check` runs check-suite and extracts the count (recorded).
    - Validation: cheapest first — node --check; the three acceptance invocations; pre-commit temp-repo test.
    - Acceptance evidence: baseline JSON, transcripts, test output.
    - Repair attempts: 0
    - Recovery note: standalone script + one lefthook job; JSON disposable (re-record).
    - (D12) Retargeted to .lefthook.yml per the journal-learnings superseded-pointer.

12. [pending] Node 22 toolchain helper
    - Task ID: J3
    - Depends on: none
    - Parallel group: G1 (Wave A)
    - Risk: standard
    - Owned scope: `scripts/with-node22.mjs` (new), THIS plan's Verification Strategy PATH-prepend prose (replaced by the helper invocation), the directory-form `node --test <dir>` audit + glob-form fixes in test script invocations ONLY (no package.json/lockfile edits — F-060 is re-homed, D14)
    - Not in scope: csm-browse/package.json + pnpm-lock.yaml (R3); bootstrap/package.json (R7); host Node upgrade; ~/.npmrc; CI
    - Spike candidate: none — the PATH-prepend recipe is proven
    - Actions: port the journal-learnings T003 verbatim minus the package manifest edits: `--print` resolves node (prefer `$HOME/.nvm/versions/node/v22.23.2/bin/node`, else current node >=22 <25, else exit 1 with fix instructions); `--exec <cmd...>` re-runs with PATH prepended; audit all `node --test <dir>` sites (incl. no-trailing-slash forms) and convert to glob/explicit forms; replace this plan's PATH-prepend prose with the helper invocation. Record F-060's re-homing (csm-browse -> R3 verification, bootstrap floor -> R7).
    - Acceptance signal: `node scripts/with-node22.mjs --print` prints a node >=22 <25 path (exit 0); `node scripts/with-node22.mjs --exec node --version` matches; an audit of the real invocation sites (Makefile, `.lefthook.yml`, csm-browse/package.json scripts, and every non-COMPLETE plan's acceptance signals) returns no directory-form `node --test <dir>` matches (glob/explicit forms only); csm-browse `npm test` green under the helper.
    - Validation: cheapest first — node --check; --print/--exec; the rg audit; csm-browse tests.
    - Acceptance evidence: outputs, audit result, test output.
    - Repair attempts: 0
    - Recovery note: additive; nvm-path-absent falls back to the current node with the version guard.
    - (D14) No package-manifest edits here; F-060 recorded as re-homed.

13. [pending] Deferred-work ledger + DEFERRED-citation rule
    - Task ID: J4
    - Depends on: J1
    - Parallel group: G2 (Wave B)
    - Risk: standard
    - Owned scope: `.agents/docs/deferred.md` (new), `scripts/check-suite.mjs` (DEFERRED-citation sub-check), `.agents/README.md` (ledger index line — sole owner)
    - Not in scope: the deferred items themselves; other check-suite rules
    - Spike candidate: none
    - Actions: port the journal-learnings T004 verbatim: the 5 ledger records (DEF-CI, DEF-SANDBOX, DEF-README, DEF-EVAL, DEF-RELEASE) with rationale + owning plans + user-decision dates; plans-corpus sub-check: any `[blocked] DEFERRED` task in a NON-COMPLETE plan must cite `[DEF:<slug>]`; COMPLETE plans warn only; planted-defect test with the specific message.
    - Acceptance signal: check-suite exits 0 on the real corpus AND the planted temp corpus exits 1 with the specific DEFERRED-citation message.
    - Validation: cheapest first — node --check; planted run; real run; `rg '\[DEF:' .agents/plans/*.md`.
    - Acceptance evidence: gate outputs, deferred.md content, README diff.
    - Repair attempts: 0
    - Recovery note: additive; never fails COMPLETE plans; fix plan citations, never the rule.
    - (Spec source: journal-learnings T004.)

14. [pending] Plan acceptance-signal lint
    - Task ID: J5
    - Depends on: J4
    - Parallel group: G3 (Wave C)
    - Risk: high (parses every non-COMPLETE plan's signals; must not false-positive)
    - Owned scope: `scripts/check-plan-signals.mjs` (new), `scripts/check-suite.mjs` (plan-corpus sub-check wiring — Wave C owner), `scripts/lib/contracts.mjs` (if constants needed)
    - Not in scope: COMPLETE plans (exempt); the F-003 harness (R8)
    - Spike candidate: none — failing patterns documented; corpus = all non-COMPLETE plans (this draft + the reopened consolidated plan + any in_progress); fix genuine violations in those plans' signals (content edits, never status lines)
    - Actions: port the journal-learnings T005 verbatim: extract bash acceptance signals (fenced + inline under `Acceptance signal:` lines, fence-aware; reuse the canonical splitLines/fenceMap from plan-validation.mjs), bash -n each, reject `<...>` placeholders, `; test $? -eq` under set -e, `grep -q "$m"` over dash-leading tokens; wire into check-suite's plans corpus block; planted-defect test with all four failure classes + specific messages (temp-dir corpus — the F-003 harness lands later in R8).
    - Acceptance signal: `node scripts/check-plan-signals.mjs .agents/plans` reports all non-COMPLETE plans PASS AND the planted temp plan fails with the specific messages (recorded).
    - Validation: cheapest first — node --check; non-COMPLETE corpus run; planted run; check-suite full (count delta recorded).
    - Acceptance evidence: lint output, planted output, check-suite output, corrected-plan diffs.
    - Repair attempts: 0
    - Recovery note: additive; a false positive fixes the lint rule, never the plan content to appease; temp-dir plants (harness lands in R8).
    - (D9) Uses temp-dir planted-defect tests.

15. [pending] Plan closure automation
    - Task ID: J6
    - Depends on: none
    - Parallel group: G1 (Wave A)
    - Risk: standard
    - Owned scope: `scripts/close-plan.mjs` (new); exercised only in temp dirs during this build
    - Not in scope: any real plan closure; check-suite.mjs
    - Spike candidate: none — the D9 closure convention is fully specified in the consolidated plan's Assumptions And Decisions
    - Actions: port the journal-learnings T006 verbatim: Control rewrite (Status complete, state NOT_STARTED, Next transition none; closed as superseded), `## Closure` block insertion, task-line `[blocked]` rewrites, journal row, `.agents/README.md` line update, check-suite re-run; `--dry-run` prints the full diff; planted temp-corpus real run (sandbox only).
    - Acceptance signal: `node scripts/close-plan.mjs --dry-run "$TMPDIR/tmpclose/plan.md" "$TMPDIR/tmpclose/replacement.md"` (files copied into the sandbox first) produces a diff containing all six elements AND check-suite passes on the temp corpus root after a real run inside the sandbox.
    - Validation: cheapest first — node --check; dry-run diff; temp-corpus real run.
    - Acceptance evidence: dry-run diff, temp-corpus check-suite output.
    - Repair attempts: 0
    - Recovery note: dry-run default; sandbox-only real run; if the temp gate fails, fix the script, never the fixtures.
    - (Spec source: journal-learnings T006.)

16. [pending] Journal-lessons reference doc
    - Task ID: J7
    - Depends on: none
    - Parallel group: G1 (Wave A)
    - Risk: low
    - Owned scope: `.agents/docs/journal-lessons.md` (new), `.agents/README.md` (docs-section index lines for journal-lessons.md + gate-baselines.json — sole writer for those lines; the deferred.md line is J4's)
    - Not in scope: check-suite/docs gating; any plan content
    - Spike candidate: none
    - Actions: port the journal-learnings T007 verbatim: the 43 mined themes grouped by class with evidence citations, frequency, and the embracing mechanism (this plan's task IDs; "already embraced" markers); rejected themes marked with rationale; README index lines.
    - Acceptance signal: the doc exists, is indexed, and `node scripts/check-suite.mjs` still exits 0.
    - Validation: cheapest first — check-suite; README read-back; theme-count spot-check.
    - Acceptance evidence: the doc, README diff, check-suite output.
    - Repair attempts: 0
    - Recovery note: doc-only; revertible.
    - (Spec source: journal-learnings T007.)

## Verification Strategy
- Fast per-task gates: `node --check` on every touched .mjs; each task's acceptance signal (cheapest first, glob-form node --test); component suites after the owning tasks.
- Order: per-task acceptance -> affected component suite -> `node scripts/check-suite.mjs` after every check-suite-mutating task (J1, J4, J5, R8) with the 585-baseline + delta recorded.
- All `node --test` invocations use glob or explicit-file forms (Node 22 quirk); all commands run under node >=22 (via J3's helper when the host default is v20).
- Parallel-safe: Wave A tasks are disjoint per the ownership table; the intra-finding splits (F-021, F-067, F-068, F-065) are file/component-partitioned (D8/D16).
- Acceptance coupling (F19): several acceptances run shared suites (csm-browse unit glob, check-suite, pack) — INTEGRATE re-runs each task's acceptance after its Wave-A siblings settle; never trust a green acceptance captured while a sibling held the same suite red.
- Final gate (after R8): full battery — check-suite (585 + deltas), sync --check, matrix --check, plan-signal lint on the non-COMPLETE corpus, check-suite harness suite, resume-semantics, five bootstrap suites serial, csm-browse unit (glob form) + e2e-REQUIRE probe, csm-scan serial, upload tests, make lint (oxlint present), `node scripts/pack-bootstrap.mjs` re-run + the per-skill diff loop, `git status --porcelain` intended-set review.
- Environment sensitivity: Node >=22 for all gates (J3 helper); Docker only for the F-001 spike + the e2e REQUIRE probe (both record environment-blocked when Docker is down); no network except the F-057 tracking note; pnpm installs only in csm-browse (R3).

## Risks And Recovery
- Check-suite chain contention: mitigated by the strict J1->J4->J5->R8 serialization; a failed chain task blocks forward motion, never parallel edits.
- Ownership collisions: eliminated by the per-file table; any future split must be recorded in the table before editing.
- F-001 regression risk: spike first, unit gate on the bare-probe rejection, existing e2e preserved.
- F-045/46/49 trust semantics: conditional-when-present verification keeps the no-signature local flow working; hard-requirement deferred to T009 — recorded, never silently dropped.
- F-064 TLS-key history purge: default = ephemeral regen + forward deletion; purge only with explicit user approval at RECOVER — residual recorded otherwise.
- Plan-signal lint false positives: run against the non-COMPLETE corpus first, fix genuine violations, never weaken the lint.
- csm-scan test surgery (R5) breaking the serial suite: full serial suite is the authoritative gate; revert per-file, never weaken a gate to appease.
- Payload drift gate false positives: fix the mapping, never the payload.
- F-053 untracked-filter vacating planted corpora: gated on `.git` presence (D15).
- Rollback: every task is independently revertible; the plan is checkpointed per cycle.
- Forward recovery: R8's harness de-risks future check-suite work; J2's baselines make journal numbers machine-verifiable.

## Critique Resolution
| Finding | Severity | Resolution | Evidence |
|---|---|---|---|
| R1/R9 double-assignment (docker/session-daemon/ensure-browser; same defects) | critical | F-065-a/c/d folded into R1; R9 scoped to pack-bootstrap + auth/security test edits | Ownership table; D8 |
| R3/J3 lockfile clash + incoherent F-060 | critical | J3 drops manifest edits; F-060 re-homed (csm-browse->R3 verify, bootstrap->R7); bogus bootstrap-lockfile instruction deleted | D14 |
| Four-way csm-browse/tests/unit + three-way csm-scan/test ownership | major | Per-file table; R3 fuzz in a NEW fuzz.test.mjs; R9 owns auth/security only; R5 owns all scan test edits | D16; table |
| Wrong paths (lib/ensure-browser, package/package.json, render/write.mjs) | major | Paths corrected; F-024's write.mjs lines + F-065-b single-owned by R4 | Current-State Evidence |
| R1/R3 constants.mjs (F-059 IMAGE) | major | F-059 constants edit -> R1; R3 keeps the e2e probe | Table |
| e2e.mjs/serve.mjs unowned + R3/R9 overlap | major | e2e.mjs + serve.mjs fully to R3 (incl. F-068 bridge detection) | D16 |
| package-audit/offline/upload test collisions (R6/R7/R9) | major | F-068 upload+integration-tier -> R6; offline -> R7 | D16 |
| F-024 write.mjs mis-ownership | major | R4 owns csm-scan/lib/scan/write.mjs (F-024 + F-065-b) | Table |
| R4 missing five deep/ modules | major | deep/{config,conventions,testing,documentation,operations}.mjs added to R4 | R4 scope |
| F-067 six unowned components | major | browse.mjs/cdp.mjs/log.mjs added to R1; sweep sid-validation added to R2; constants/port-lock/cgroup to R1 | R1/R2 scopes |
| F-069 component 2 silently dropped | major | Recorded already-resolved via the lefthook migration | D11 |
| F-060 sketch remainder (ceiling/engine-strict) | major | Recorded carve-out (publication-gated) | D14 |
| R1/R2/R3 acceptance signals directory-form `node --test` | major | Glob form mandated in all signals | Constraint; critic empirical proof |
| R7 `diff -rq csm-*` not runnable | major | Per-skill diff loop in the acceptance signal | R7 signal |
| J2 acceptance signal lost the script prefix | minor | Restored `node scripts/record-gate-baseline.mjs` prefix | J2 signal |
| J3 rg pattern misses no-trailing-slash forms | nit | Pattern widened; no-directory-form constraint added | J3 signal |
| R9 "four suites then five" | nit | Acceptance reworded to "the affected suites listed" | R9 signal |
| J1/J4/J5 planted tests vs F-053 untracked filter | major | F-053 gated on `.git` presence (D15) | R8 constraint |
| R8 dependency edge (pre-commit.test.mjs co-edit with J2; node>=22) | minor | Wave serialization + note; harness runs under J3 resolution | R8 note |
| J5 high-risk omitted from summary/AC6 | minor | Risk summary + AC6 now list 4 high (R1, R7, R8, J5) | Control; AC6 |
| R7 scope omits tests/integration/bootstrap-flow.test.mjs | nit | Added to R7 scope | Table |
| F-016 seccomp component unrecorded | nit | Docker-default-seccomp note added to R1 action | R1 action |
| F-068 scan components unowned | major | Moved to R5 (determinism sort-key + self-digest) | D16; R5 scope |

## Progress Journal
| Timestamp | Cycle | Transition | Tasks | Evidence/result | Next state |
|---|---|---|---|---|---|
| 2026-08-20 | 0 | INTAKE -> DISCOVER -> RESEARCH | — | Consolidated-plan reopen hygiene landed (207bd13); T012 verified complete via sibling plan; three parallel scouts extracted all 68 finding sketches + current-state verdicts; journal-learnings plan read fully; baseline 585 checks green; F-060 re-homed; real paths verified | DRAFT |
| 2026-08-20 | 0 | DRAFT -> CRITIQUE | — | 16 tasks drafted; hostile critique returned 26 findings (3 critical ownership collisions, 10 major, rest minor/nit) | REMEDIATE |
| 2026-08-20 | 0 | REMEDIATE -> CRITIQUE (round 2) | — | Round-2 fresh-eyes critique returned 19 findings (NO-GO): diff-loop subset mismatch, R9/R1 docker.mjs double-ownership + unowned test files, F-067-1 mislabeled (dead recorder guard, not F-065-a), F-069-6 actually OPEN at HEAD, runtime-commands.json out of R8 scope, Wave-A pack-serial contradiction, wrong lib/verbs + lib/cleanup paths, J1 live-acceptance ordering, five-suite command prose, Control bullets, table completeness, J6 metavariables, J2 baseline race, J3 audit targets | REMEDIATE |
| 2026-08-20 | 0 | REMEDIATE -> VERIFY | — | All 19 round-2 findings folded in: mapping-aware diff (F1), docker.mjs seam extraction -> R1 + caller re-points -> R9 (F2), F-067-1 dead-guard action (F3), F-069-6 -> R4 (F4), runtime-commands.json in R8 scope+table (F5), R9 sandboxed pack-failure simulation (F6), J1 INTEGRATE re-verification note (F7), lib/verbs + lib/cleanup paths (F8), F-059 action in R1 (F9), exact five-suite command (F10), Control Last model/run + Resume (F11), table rows for harness/contracts (F13), concrete J6 paths (F14), J2 re-record note (F15), R3 wording (F16), PENDING_DEBT cite (F17), J3 audit targets (F18), acceptance-coupling note (F19) | VERIFY |

## Completion Review
(filled by csm-build when all criteria are verified)
