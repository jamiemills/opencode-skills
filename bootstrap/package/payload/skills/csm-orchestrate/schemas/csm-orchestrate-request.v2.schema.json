{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "csm-orchestrate-request/2",
  "title": "CSM orchestrate request envelope",
  "description": "Additive revision of the immutable driver intake request envelope. Carries the /1 identity, verb (kind), and routing hints, plus an optional decision block selecting the host-mediated typed-decision mode (off/shadow/live) and bounding the enabled decision points. Decision data is advisory/apply input only and never enters acceptance gates.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "schemaRevision", "requestId", "runId", "kind", "prompt", "goalSlug"],
  "properties": {
    "schema": { "const": "csm-orchestrate-request/2" },
    "schemaRevision": { "const": 2 },
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "Non-empty identifier for this request; runId (not requestId) is the canonical identity consumed by lease/evidence dirs and handoff."
    },
    "runId": { "$ref": "#/$defs/runId" },
    "kind": {
      "enum": [
        "research",
        "grill",
        "plan",
        "execute-plan",
        "bdd-tdd",
        "review",
        "review-python",
        "tests",
        "scan",
        "ddd",
        "browse",
        "upload",
        "autoresearch"
      ],
      "description": "Skill-taxonomy verb the request routes on; csm-orchestrate has no self-route."
    },
    "prompt": {
      "type": "string",
      "minLength": 1,
      "description": "Free-text instruction that activates the routed skill."
    },
    "goalSlug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Canonical worktree goal slug (matches wt-session SLUG_RE); feeds worktree/dir naming."
    },
    "artifactRef": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Optional reference to a prior artifact (e.g. a saved plan or approach) the routed skill should consume."
    },
    "repo": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Optional repository path or identifier the request targets; absent when the request is repo-agnostic."
    },
    "constraints": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Optional bounded constraints narrowing the routed skill's execution."
    },
    "requestedSignals": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional explicit-mode routing hints; a skill named here is selected even when its activation mode is explicit-only."
    },
    "decision": {
      "$ref": "#/$defs/decision",
      "description": "Optional typed-decision opt-in: off (default) records nothing, shadow records advice only, live may apply reversible non-safety points. Absent means the layer is disabled."
    }
  },
  "$defs": {
    "runId": { "type": "string", "pattern": "^run-[a-z0-9][a-z0-9-]{1,127}$" },
    "decision": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode"],
      "properties": {
        "mode": {
          "enum": ["off", "shadow", "live"],
          "description": "Decision-layer activation: off disables, shadow records advice, live may apply guarded reversible points."
        },
        "points": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 },
          "uniqueItems": true,
          "description": "Optional decision-point ids to enable; absent means the default point set for the mode."
        }
      }
    }
  }
}
