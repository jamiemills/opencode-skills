{
  "name": "csm-browse",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "for test_file in tests/unit/*.test.mjs; do node --test --test-concurrency=1 \"$test_file\" || exit 1; done",
    "check": "node scripts/check-skill.mjs"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.34.0"
  },
  "devDependencies": {
    "ws": "^7.5.13"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "packageManager": "pnpm@10.34.5"
}
